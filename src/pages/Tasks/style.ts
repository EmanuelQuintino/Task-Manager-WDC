import styled from "styled-components";

export const Container = styled.main`
  margin-top: 2.4rem;

  h2 {
    font-size: 3.2rem;
  }

  .tasksContainer {
    height: 65vh;
    overflow-y: auto;
  }

  @media (min-width: 768px) {
  }
`;
